export enum ResilocProxyMetadataType {
    Required = 'required',
    Static = 'static',
    Default = 'default',
}

export enum TypeOfDataType {
    Required = 'boolean',
    String = 'string',
    Number = 'number',
    Date = 'date',
    Text = 'text',
    GeoJSON = 'geoJSON',
}

export enum AvailabilityType {
    Measured = 'measured',
    Derived = 'derived',
    NotAvailable = 'not_available',
    NotApplicable = 'not_applicable',
}